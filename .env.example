# Transport: "sse" for HTTP/SSE, omit (or anything else) for stdio
MCP_TRANSPORT=sse

# Port for SSE transport (default: 8080)
PORT=8080

# Add your provider env vars here:
# MY_SERVICE_URL=http://localhost:9090
# MY_SERVICE_API_KEY=changeme
